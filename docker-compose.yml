version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - DEBUG=${DEBUG:-true}
    volumes:
      - ./uploads:/app/uploads
      - ./frontend:/app/frontend
    env_file:
      - .env
    restart: unless-stopped
